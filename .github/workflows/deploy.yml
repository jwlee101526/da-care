name: Verify production

on:
  push:
    branches: [production]
  workflow_dispatch:

concurrency:
  group: production-verify
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify:
    uses: ./.github/workflows/ci.yml
